# 远端下载验收

验收时间（UTC）：2026-09-08T14:05:26.990744+00:00

通过 `python learn.py clone` 从公开 GitHub 仓库下载 `projects.json` 指定提交，随后执行 `python learn.py verify`。三个仓库均为指定提交且没有未提交修改。

| 工程 | 测试数 | 演示 |
|---|---:|---|
| agent_ctrip_assistant | 13 | 预订提案、审批/拒绝、关闭数据库后恢复 |
| RAG | 5 | 三道合成问题的预期来源均命中 |
| Text2SQL | 22 | 六个实际 SQLite 查询执行成功 |

合计 **40 项测试通过，6/6 项运行检查通过**。全部离线使用合成数据，模型协议通过测试替身验证，未做真实模型效果验收。

三个工程的逐章快照也在发布准备阶段分别运行对应的章节示例。远端持续集成结果见各仓库 Actions，总入口 CI 会重新下载固定提交并重复这套验证。

ERP 的完整依赖和端到端部署遵循其 README；这次仅更新其配套学习文档，没有修改业务代码。
